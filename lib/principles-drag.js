(() => {
  "use strict";

  // Node movement is owned by the React Flow Principles component.
  // This legacy file remains as a harmless compatibility stub because older
  // reading-page HTML may still request it from cache.
  window.__TJM_REACT_FLOW_LEGACY_DISABLED = true;
})();
