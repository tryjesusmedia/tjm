(() => {
  "use strict";

  // React Flow separates node dragging from node actions, so the former
  // capture-phase click guard is no longer needed. Keep this compatibility
  // file inert for older cached page markup.
  window.__TJM_REACT_FLOW_GUARD_DISABLED = true;
})();
